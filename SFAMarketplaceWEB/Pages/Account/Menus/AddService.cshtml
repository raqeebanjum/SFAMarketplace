@page
@model SFAMarketplaceWEB.Pages.Account.Menus.AddServiceModel
@{
    Layout = HttpContext.User.Identity.IsAuthenticated ? "_LayoutAuthenticated" : "_Layout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/AddServiceStyleSheet.css" />
}

<div class="service-form-container">
    <h2 class="form-title">Add New Service</h2>
    <form method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="NewService.ServiceName" class="form-label">Service Name:</label>
            <input asp-for="NewService.ServiceName" class="form-control" />
            <span asp-validation-for="NewService.ServiceName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="NewService.ServiceDescription" class="form-label">Description:</label>
            <textarea asp-for="NewService.ServiceDescription" class="form-control"></textarea>
            <span asp-validation-for="NewService.ServiceDescription" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="NewService.ServiceRate" class="form-label">Service Rate($) / hour:</label>
            <input asp-for="NewService.ServiceRate" type="number" step="0.01" class="form-control" />
            <span asp-validation-for="NewService.ServiceRate" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-primary"><i class="bi bi-check2-circle"></i> Submit</button>
    </form>
</div>
